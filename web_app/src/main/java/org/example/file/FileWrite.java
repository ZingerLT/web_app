package org.example.file;

import org.springframework.stereotype.Component;

@Component
public class FileWrite {

   /* @Autowired
    Json obj;

    FileWriter writer = new FileWriter("file.txt", true);

    public FileWrite() throws IOException {
        writer.write(obj.GetInfo());
        writer.close();
    }*/
}
